pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'connect jenkins to github via URL'
            }
        }
        stage('Build') {
            steps {
                echo 'Sample build step'
            }
        }
    }
}
